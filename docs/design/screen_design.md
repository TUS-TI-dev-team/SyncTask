# Screen Design (画面構成)

## ログイン関係

| 画面名 | 役割・機能 | 遷移先 | 構成要素 |
| --- | --- | --- | --- |
| ログイン画面 | メールアドレスとパスワードの入力＆認証 | パスワードリセット/メールアドレス入力画面<br>アカウント作成/情報入力画面<br>ホーム画面 | メールアドレス入力欄<br>パスワード入力欄(マスク在り・表示も可能)<br>「ログイン」ボタン<br>「アカウント作成/情報入力画面」へのリンク<br>「パスワードリセット/メールアドレス入力画面」へのリンク |
| アカウント作成/情報入力画面 | アカウント情報（ユーザー名、パスワード、メールアドレス）を入力する<br>アカウントを新規に作成 | アカウント作成/OTP入力画面<br>ログイン画面 | ユーザー名入力欄<br>メールアドレス入力欄<br>パスワード入力欄(マスク在り)<br>パスワード確認用入力欄(マスク在り)<br>「続行」ボタン<br>「ログイン画面」へのリンク |
| アカウント作成/OTP入力画面 | 送信先情報（始め4文字, ドメインのみ表示し他はマスクで隠す + 文字数が予測できないようマスクは10文字分とする）およびOTP入力欄を表示。<br>OTP検証成功（アカウント作成確定）時、自動的にログインセッションを発行してホーム画面へ遷移する。<br>15分のOTPセッション期限が切れた場合は自動的にアカウント作成画面/情報入力画面へリダイレクトされる。<br>OTP発行後5分が経過した場合はOTPの有効期限が切れたことをポップアップにて通知し再送を促す。 | 決定（認証成功）：ホーム画面<br>戻る・キャンセル：アカウント作成/情報入力画面 | 15分のセッションタイマー<br>OTP入力欄<br>「戻る」ボタン<br>「再送信」ボタン（60秒クールダウン・カウントダウン表示あり）<br>「決定」ボタン |
| パスワードリセット/メールアドレス入力画面 | パスワードをリセットするユーザーのメールアドレスを入力＆認証メール送信を指示 | パスワードリセット/OTP入力画面<br>ログイン画面 | メールアドレス入力欄<br>「メール送信」ボタン<br>「ログイン画面」へのリンク |
| パスワードリセット/OTP入力画面 | 送信先情報（始め4文字, ドメインのみ表示し他はマスクで隠す + 文字数が予測できないようマスクは10文字分とする）およびOTP入力欄を表示<br>15分のOTPセッション期限が切れた場合は自動的にアカウント作成画面/情報入力画面へリダイレクトされる。<br>OTP発行後5分が経過した場合はOTPの有効期限が切れたことをポップアップにて通知し再送を促す。 | 決定（認証成功）：パスワード再設定画面<br>戻る・キャンセル：パスワードリセット/メールアドレス入力画面 | 15分のセッションタイマー<br>OTP入力欄<br>「戻る」ボタン<br>「再送信」ボタン（60秒クールダウン・カウントダウン表示あり）<br>「決定」ボタン |
| パスワードリセット/パスワード再設定画面 | 新しいパスワード・確認用のパスワードを入力<br>15分以内に操作が完了していない場合はセッション切れとしてパスワードリセット/メールアドレス入力画面へ戻るよう表示する<br> | ログイン画面<br>パスワードリセット/メールアドレス入力画面 | 15分のセッションタイマー<br>新パスワード入力欄(マスク在り)<br>新パスワード確認用入力欄(マスク在り)<br>「決定」ボタン |

## ホーム画面

| 画面名 | 役割・機能 | 遷移先 | 構成要素 |
| --- | --- | --- | --- |
| ホーム画面 | 高優先度タスク一覧表示(デフォルトビュー)、締切間近タスク表示、ピン止めタスク表示<br>の3つのタスクビューを、切り替えて表示できるようにする<br>ビューはリスト表示（カレンダーではない）<br>タスク作成・編集・状態変更・削除もできるようにする | タスク作成ポップアップ<br>タスク編集ポップアップ | タスク一覧表示欄（タスク編集、タスク状態変更、削除、ピン止めが可能）<br>「高優先度タスク一覧表示」ボタン<br>「締切間近タスク表示」ボタン<br>「ピン止めタスク表示」ボタン<br>「完了タスク表示/非表示」切り替えトグル<br>ページネーション操作UI（前へ/次へボタン、ページ番号）<br>タスク作成ボタン |

### ページネーションの補足
- ページネーションUIはタスクリストの上下に配置する
- ホーム画面，タスク一覧画面にてページネーション操作UIのページ番号表示について下記の仕様に変更
   - 全ページ数Nが10を超える(N>10)の場合
      - 現在のページnに対して: [1, 2, ..., n-1, n, n+1, ..., N]のページ数のみ表示する．ただしNが下記の場合は下記とする
        - n == 1 -> [1, 2, 3, ..., N]
        - n ==N -> [1, 2, ..., N-2, N-1, N]
   - 全ページ数Nが10以下の(N<=10)場合: すべてのページを表示する

## タスク関連

| 画面名 | 役割・機能 | 遷移先 | 構成要素 |
| --- | --- | --- | --- |
| タスク一覧画面 | タスクの表示、検索および作成、編集、状態変更、削除、ピン止めが可能<br>表示方式の切り替えも可能（リスト表示、カレンダー表示） | タスク作成ポップアップ<br>タスク編集ポップアップ | タスク一覧表示欄（リスト表示部 / カレンダー表示部）<br>検索用入力欄（キーワード）<br>優先度絞り込みプルダウン<br>ステータス絞り込みプルダウン<br>締切日絞り込み入力欄<br>「完了タスク表示/非表示」切り替えトグル<br>ページネーション操作UI（前へ/次へボタン、ページ番号）<br>「表示形式切り替え」ボタン（リスト表示 / カレンダー表示）<br>カレンダー表示切替UI（全体表示［月グリッド］ / 週表示）<br>カレンダーナビゲーション（全体表示時:「前月（＜）」「次月（＞）」「今日」ボタン）<br>カレンダーナビゲーション（週表示時:「前週（＜）」「翌週（＞）」「今日」ボタン）<br>「タスク作成」ボタン |
| タスク作成ポップアップ | タスクの属性を入力して作成する<br>（期間と曜日を指定した毎週タスクの自動作成［即時一括生成］も可能） | もとの画面 | タスク名入力欄<br>優先度選択プルダウン<br>締切り日時入力欄<br>コメント入力欄<br>「繰り返し作成」切り替えトグル/チェックボックス<br>期間指定（開始日・終了日）入力欄（カレンダーピッカー付き）<br>締め切り時刻(任意指定)<br>曜日選択チェックボックス（日〜土）<br>「決定」ボタン<br>「キャンセル」ボタン |
| タスク編集ポップアップ | タスクの属性を入力して更新する<br>（※個別タスクの更新のみであり、繰り返しタスクの一括連動更新機能は提供しない） | もとの画面 | タスク名入力欄<br>ステータス選択プルダウン<br>優先度選択プルダウン<br>締切り日時入力欄<br>コメント入力欄<br>「決定」ボタン<br>「キャンセル」ボタン |

### タスク操作補足
- タスク削除時は、確認ダイアログを表示
- タスク状態更新は、ボタン一つで更新できるようにする
- タスクピン止め状態は、ボタン一つで更新できるようにする
- 高優先度・ピン止めタスク表示およびカレンダー表示等では「完了タスク表示/非表示」切り替えトグルスイッチを提供（※「締切間近タスク表示」選択時は、要件上完了タスクは常に除外されるため、「完了タスク表示/非表示」切り替えトグルは非活性［Disabled］または非表示制御とする）
- **タスク作成（繰り返し自動作成）**:
  - 「繰り返し作成」有効時は、期間（開始日・終了日: YYYY/MM/DD）および曜日（月〜日）を指定し、範囲内の該当曜日ごとにタスクを即時一括生成する（最大100件/52週以内）。
  - 自動作成されたタスクは個別独立した通常タスクとしてDB登録され、タスク編集画面では個別タスクの編集のみを対象とする（一括連動更新は非提供）。
- **カレンダー表示および日付詳細ポップアップ**:
  - カレンダー表示形式では「全体表示（5〜6週グリッド）」と「週表示（7日間）」を切り替え可能とし、全体表示は「前月（＜）」「次月（＞）」「今日」、週表示時は「前週（＜）」「翌週（＞）」「今日」ボタンによるナビゲーションを提供する。
  - カレンダーグリッドの日付セルをクリックするとモーダル「日付詳細ポップアップ」が表示される。
    - ポップアップ構成要素: 該当日付、タスク一覧（タイトル・締切時刻・ステータス・優先度・コメント）、ステータス直接変更UI（未着手/進行中/完了）、タスク編集画面へのリンク、ピン止めUI、該当日締切「新規タスク作成」ボタン、閉じるボタン
    - ポップアップ遷移先: タスク編集画面、タスク作成画面（該当日付を締切日に事前入力）
  - カレンダーの日付セル上およびポップアップ内の双方でタスクステータス、ピン止めを直接変更可能（セル上でのタスク操作時は日付詳細ポップアップ展開イベントを抑止する）。

## アカウント関連

| 画面名 | 役割・機能 | 遷移先 | 構成要素 |
| --- | --- | --- | --- |
| プロフィール表示画面 | アカウント情報（ユーザー名、メールアドレス）を表示 | プロフィール編集画面<br>パスワード変更画面<br>アカウント削除/パスワード再認証画面 | アカウント情報を表示<br>「プロフィール変更」ボタン<br>「パスワード変更」ボタン<br>「アカウント削除」ボタン |
| プロフィール編集画面 | アカウント情報（ユーザー名、メールアドレス）の入力欄を表示<br>メールアドレスの変更があった場合は、メール送信及びログイン画面へ戻る点についての確認ダイアログを出したのちメール変更手続き（OTP送信）に進む | メール変更無し：プロフィール表示画面<br>メール変更あり：アカウント関連/OTP入力画面 | 「ユーザ名」入力欄(デフォルト値として現在のアカウント情報を表示)<br>「メールアドレス」入力欄(デフォルト値として現在のアカウント情報を表示)<br>「キャンセル」ボタン<br>「決定」ボタン |
| アカウント関連/OTP入力画面 | 新メールアドレス宛に送信されたOTPを入力する。<br>OTP検証成功（メール変更確定）時、全ログインセッションが物理削除されるためログイン画面へリダイレクトし、新メールアドレスでの再ログインを要求する。<br>15分のOTPセッション期限が切れた場合は自動的にアカウント作成画面/情報入力画面へリダイレクトされる。<br>OTP発行後5分が経過した場合はOTPの有効期限が切れたことをポップアップにて通知し再送を促す。 | 決定（認証成功）：ログイン画面<br>戻る・キャンセル：プロフィール編集画面 | 15分のセッションタイマー<br>OTP入力欄<br>「戻る」ボタン<br>「再送信」ボタン（60秒クールダウン・カウントダウン表示あり）<br>「決定」ボタン |
| パスワード変更画面 | 現在のパスワードと新しいパスワードを入力し、パスワードを変更する。<br>パスワード変更完了時、全ログインセッションを一括物理削除してログイン画面へリダイレクトし再ログインを要求する。<br>5回連続で既存パスワード認証に失敗した場合もログインセッションを破棄してログイン画面にリダイレクト。 | 変更完了・5回連続失敗：ログイン画面<br>キャンセル：プロフィール表示画面 | 現在のパスワード入力欄(マスク在り)<br>新しいパスワード入力欄(マスク在り)<br>新しいパスワード（確認）入力欄(マスク在り)<br>「決定」ボタン<br>「キャンセル」ボタン |
| アカウント削除/パスワード再認証画面 | 本人確認のため現在のパスワードを入力し、アカウントを削除する。<br>5回連続でパスワード再認証に失敗した場合はログインセッションを破棄（物理削除）してログイン画面にリダイレクトする。<br>パスワード認証成功時は本当に削除するのか確認するポップアップを表示する。 | 削除実行・5回連続失敗：ログイン画面<br>キャンセル：プロフィール表示画面 | 削除確認メッセージ<br>現在のパスワード入力欄(マスク在り)<br>「削除」ボタン<br>「キャンセル」ボタン |

## ヘッダー・フッター

| 部品名 | 役割・機能 | 遷移先 | 構成要素 |
| --- | --- | --- | --- |
| ヘッダー | ログイン後画面の上部に表示<br>※未ログイン時は表示しない。<br>ログアウト時はセッション破棄の上でログイン画面へリダイレクト。 | ホーム<br>タスク一覧画面<br>プロフィール表示画面<br>ログアウト（ログイン画面へ遷移） | 「ホーム」リンク<br>「タスク」リンク<br>「プロフィール」リンク<br>「ログアウト」リンク |
| フッター | 画面下部共通フッター（特記事項なし） | なし | なし |

### OTP・セキュリティ操作補足
- 各OTP画面での送信先メールアドレス表示は「始め4文字＋ドメイン名のみ表示、他10文字固定幅マスク」とする。
- 「再送信」ボタン押下直後は60秒間のクールダウン期間が設定され、ボタンが非活性化されるとともにカウントダウンタイマーが表示される。

### 共通UI・エラー表示補足
- 各種入力フォーム画面（ログイン、新規登録、パスワードリセット、プロフィール編集、パスワード変更、アカウント削除、タスク作成・編集等）においては、入力バリデーションエラーやAPIエラー（認証失敗・ロックアウト・サーバーエラー等）を明示するためのエラーメッセージ表示領域（各入力欄下部のインラインエラー表示およびフォーム上部のアラートバナー領域）を備える。
