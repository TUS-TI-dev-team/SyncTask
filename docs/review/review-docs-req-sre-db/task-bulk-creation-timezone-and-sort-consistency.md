# 毎週タスク自動作成における一括生成順序および同一作成日時のソート決定性に関する仕様補足

- **Status**: Open
- **Severity**: Minor
- **Created At**: 2026-08-18 19:55:00
- **Target Files**:
  - [requirements.md](docs/req-def/requirements.md)

## 1. 問題の概要
期間・曜日指定による「毎週タスク自動作成（即時一括生成方式）」において、複数件のタスクが単一トランザクションで一括生成される際のDB登録順序（日付昇順等）や、作成日時（`created_at`）が同一ミリ秒となった場合のソート順決定性についての仕様が明記されていません。

## 2. 詳細な指摘内容
`docs/req-def/requirements.md` の L85-94 に以下の記述があります：

```markdown
		- 期間と曜日を指定し、その範囲内で毎週タスクを自動作成できる（即時一括生成方式）
			- 期間指定: 開始日と終了日をそれぞれYYYY/MM/DD形式で入力（テキスト入力またはポップアップ表示のカレンダーから選択可能）
			- 期間・生成件数バリデーション:
				- 開始日 <= 終了日（開始日と終了日が同一日の場合も指定曜日に合致すれば1件作成可能）
				- 開始日には過去日も指定可能
				- 指定可能な最大期間は1年間（最大52週）以内、かつ計算される生成タスク数は1件以上100件まで（期間内に選択された曜日が1日も含まれず生成件数が0件となる場合は「指定された期間内に該当する曜日が存在しません」とエラー表示し処理を中断する。また、期間および選択曜日の組み合わせにより生成予定件数が100件を超える場合は送信前に警告または送信時に「生成件数が上限（100件）を超えています」とエラー表示し、タスクの一括作成処理を一切実行せず中断する［All-or-Nothing］）
			- 曜日指定: 「日、月、火、水、木、金、土」より1つまたは複数選択可能（1つ以上選択必須）
			- 毎週作成サイクル: 開始日から終了日までの期間内で、選択された該当曜日ごとに毎週タスクを生成する
			- 生成タスクの属性: 入力されたタスク名、優先度、コメント、初期ステータス（「未着手」）を各タスクへコピーする。締切時刻（hh:mm JST）は任意入力とし、指定がある場合はその時刻を各該当日付に適用し、未指定（省略）の場合は該当日の「23:59 JST」をデフォルト締切時刻として適用する
			- 生成後の個別独立性: 自動作成されたタスクはそれぞれ独立した通常タスクとしてDBに個別登録され、以降は個別に編集・状態変更・削除が可能（繰り返しタスク間の一括連動更新機能は提供しない）
```

また、一覧表示やピン留め表示等のソート順（L108, L113, L118, L125）には「締切日時が未設定または同日のタスクは作成日時の新しい順（降順）で表示」と定義されています。

### 検討すべき仕様点：
1. **生成順序**: 期間内に複数日が該当する場合（例: 毎週月・木のタスク作成）、タスクレコードはカレンダー日付順（古い日付から新しい日付への昇順）に生成・挿入されるのか。
2. **同日タスクの順序決定性**: 同一締切日・同一作成日時となったタスクが存在する場合、ソート順がDBの物理格納順やID順によってブレないよう、ソートのタイブレーク条件（例: `ORDER BY due_date ASC, created_at DESC, task_id DESC`）を意識した仕様定義が望ましいです。

## 3. 推奨される修正案
L94付近に「生成タスクは該当日付の昇順でDBに登録する」旨を補足してください：

```markdown
			- 生成タスクの登録順: 期間内の該当カレンダー日付が早い順（昇順）に各タスクを生成・DBに一括登録する（同一作成日時における表示順の決定性を担保するため）
```
